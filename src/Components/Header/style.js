import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container:{
        width: 100,
    },
    header: {
        backgroundColor: "#00ffff",
        width: "100%"
    }
})