import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container:{
        width: "100%",
        padding: 0,
        flex: 1,
        marginTop: 0,
    }
})

export default styles